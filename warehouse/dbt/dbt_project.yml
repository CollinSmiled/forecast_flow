name: forecast_flow_warehouse
version: 1.0.0
config-version: 2

profile: forecast_flow

model-paths: ["models"]
test-paths: ["tests"]
clean-targets:
  - target
  - dbt_packages

models:
  forecast_flow_warehouse:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: view
